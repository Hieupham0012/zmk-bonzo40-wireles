# Cấu hình cơ bản cho shield bonzo
CONFIG_ZMK=y

# Tăng công suất Bluetooth (nếu cần)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# TẮT USB logging (đang gây lỗi)
# CONFIG_ZMK_USB_LOGGING=y  # Dòng này gây lỗi, nên tắt
CONFIG_ZMK_USB_LOGGING=n

# Sửa dòng LOG2_MODE_DEFERRED (không tồn tại trong ZMK)
# CONFIG_LOG2_MODE_DEFERRED=y  # Dòng này sai, xóa hoặc comment
# Thay bằng (nếu cần logging):
# CONFIG_LOG_MODE_DEFERRED=y
